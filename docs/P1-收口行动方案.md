# P1 · 收口行动方案(2026-09-15)

> 澄清结论已固化到 `docs/P1-基建.md`(边界/纪律/预案)与 `docs/以终为始-工程执行清单.md`。本文件是执行序列,做完即关闭 P1。

## 决策摘要

| 议题 | 结论 |
|---|---|
| 定位 | P1 验收审计 + 补缺口后关闭 |
| ModelScope 兜底 | 手动应急预案,不写代码级 fallback |
| 协作模式 | git 单向:本地唯一写入口,云端只 `git pull` 禁 commit;pull 前脏改动三选一(回写本地/丢弃/stash,禁 `pull -f`) |
| 产物通道 | scp 回传本地归档(`D:\code\draftrouter-artifacts\<run-name>\`),sha256 校验后传输,校验一致才删云端;回传手段必须随长任务启动(云端 watch 脚本或脚本内联回调),云端只留最近 1–2 个 checkpoint |
| 数据盘 | 暂不扩容;**Qwen3-4B 草稿判显存不可行**(8B+4B fp16=22.75GiB,KV cache≈0.05GiB),7.6G 4B 缓存可删,删后余量 23.6G;>40G 触发清理 |
| 归档风险 | 本地归档为单副本(已知风险,记录在案);P1 收尾 commit 必须伴随一次 push 作代码层第二副本 |
| 骨架 | 顶层空包 `src/draftrouter/`,`tests/`、`configs/`、`pyproject.toml`(ruff)现建;子模块划分留 P2 |
| 规范 | AGENTS.md 提交规范节;不引 pre-commit;本地测试禁网络/GPU 依赖 |

## 执行序列

1. **commit ①(WP0 回填,独立逻辑单元)** — `scripts/bench`、`scripts/data_prep` 的 speculative_config 0.29 实测口径 + 数据直链修复 + `wp0_env_check.py` + ruff 清理(52 自动 + 4 手工)。消息:`fix: WP0 冒烟回填 0.29 speculative_config 口径与数据直链修复`。
2. **commit ②(P1 收口)** — `docs/P1-基建.md`、`docs/P1-收口行动方案.md`、执行清单修订、`src/draftrouter/` 空包、`tests/`、`configs/`、`pyproject.toml`、AGENTS.md 提交规范节。消息:`chore: P1 基建收口——边界文档、骨架、提交规范与 ruff 配置`。
3. **自证** — `ruff check` 全过;本地 `pytest scripts/bench/tests scripts/data_prep/tests` 全绿(基线 61 passed);派 `reviewer` 独立审查两个 commit。
4. **云端同步** — `ssh autodl "source /etc/network_turbo; cd /root/autodl-tmp/draftrouter && git pull"`;pull 前按脏改动细则处理云端残留。
5. **可选磁盘回收** — 确认 P3 前提后删 hf-cache 中 Qwen3-4B(回收 7.6G),删除动作记入当日日志。
6. **push** — 步骤 3、4 完成后请用户确认,`git push origin main`(收尾必做,兼作代码层第二副本)。

## P1 关闭判定

以上 1–4、6 完成,且 `docs/P1-基建.md` 验收清单全勾 → P1 关闭,进入 P2(开工先出 `docs/P2-*.md` 设计文档,含路由器/草稿池子模块划分)。
